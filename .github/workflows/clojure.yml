name: Clojure CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    container:
      image: clojure:openjdk-17-lein

    steps:
    - name: Checkout
      uses: actions/checkout@v3.0.2

    - name: Test
      run: |
        lein compile :all
        lein test
